eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    instance-info-replication-interval-seconds: 5
    initial-instance-info-replication-interval-seconds: 6

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JayaprakashJayavel/distributed-config.git
